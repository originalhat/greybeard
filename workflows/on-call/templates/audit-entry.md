# {TICKET-ID} — {one-line generalized summary}

> Resolved: {YYYY-MM-DD} · Repo(s): {repo} · Domain: {domain} · Type: {ops change | investigation | bug}
> Ticket: https://sanabenefits.atlassian.net/browse/{TICKET-ID}

**PHI-free.** Describe the shape of the problem, not the identifiers. The ticket link above is the pointer to the real data (JIRA has the BAA). No member/dependent names, subscriber IDs, customer group numbers, UUIDs, emails, or DOBs below.

**Symptom (generalized):**
What was reported, with identifiers removed — "a member's dependents weren't showing invite cards," not the member's name.

**Root cause:**
What was actually wrong, at the code/data level.

**Resolution:**
What was actually done (which may differ from what was first proposed — record what happened). Reference the runbook used, if any: `runbooks/{repo}/{domain}/{scenario}.md`.

**Deviation from proposal (if any):**
If the fix differed from the triage proposal, what changed and why. High-value signal for future tickets.

**Runbook impact:**
- [ ] Matched an existing runbook, worked as written (bumped History + last_verified)
- [ ] Updated an existing runbook (what changed)
- [ ] Corrected a wrong/stale runbook (what was wrong)
- [ ] Created a new runbook: `{path}`
- [ ] Self-service redirect captured
- [ ] No runbook impact

**Risk / outcome:**
Risk level of the fix and how it played out (confirmed working / awaiting vendor / monitoring).

**Escalation contacts used:**
Internal staff / teams consulted (names OK here). Useful for building out `context/ESCALATION-MAP.md`.
